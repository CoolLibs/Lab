[SDF1 SDF2] (vec3 pos) {
    float h = clamp(0.5 +0.5*(${SDF2}(pos)-${SDF1}(pos)/strength), 0.0, 1.0);
    return mix(${SDF2}(pos), ${SDF1}(pos),h) - strength*h*(1.0-h);
}

float {
    "Name": "strength",
    "Default": 0.5,
    "Min": 0.001,
    "Max": 1.0
}