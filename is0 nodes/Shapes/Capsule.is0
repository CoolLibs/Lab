[] (NodeRes ndr) {
    ndr.pos.z -= clamp(ndr.pos.z, -Length * 0.5, Length * 0.5);
    return NodeRes(ndr.pos, length(ndr.pos) - Thickness, ndr.md);
}

float {
    "Name": "Length",
    "Default": 2.0,
    "Min": 0.0,
    "Max": 10.0
}

float {
    "Name": "Thickness",
    "Default": 0.5,
    "Min": 0.0,
    "Max": 1.5
}