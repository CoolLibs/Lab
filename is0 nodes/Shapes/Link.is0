[] (vec3 pos) {
    vec3 q = vec3( pos.x, max(abs(pos.y)-lght,0.0), pos.z );
    return length(vec2(length(q.xy)-innerRadius,q.z)) - outerRadius;
}

float {
    "Name": "lght",
    "Default": 1.0,
    "Min": 0.0,
    "Max": 10.0
}

float {
    "Name": "innerRadius",
    "Default": 1.0,
    "Min": 0.0,
    "Max": 10.0
}

float {
    "Name": "outerRadius",
    "Default": 1.0,
    "Min": 0.0,
    "Max": 10.0
}