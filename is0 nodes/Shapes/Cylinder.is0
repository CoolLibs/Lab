[] (NodeRes ndr) {
    vec2 v = vec2(length(ndr.pos.xy) - Radius, abs(ndr.pos.z) - Height);
    return NodeRes(ndr.pos, v.y < 0. ? v.x : v.x < 0. ? v.y : length(v), ndr.md);
}

float {
    "Name": "Radius",
    "Default": 1.0,
    "Min": 0.0,
    "Max": 10.0
}

float {
    "Name": "Height",
    "Default": 2.0,
    "Min": 0.0,
    "Max": 10.0
}