[] (NodeRes ndr) {
	vec3 q = abs(ndr.pos) - Radius;  
	return NodeRes(ndr.pos, length(max(q, 0.)) + min(max(q.x, max(q.y, q.z)), 0.), ndr.md);  
}

float {
    "Name": "Radius",
    "Default": 1.0,
    "Min": 0.0,
    "Max": 10.0
}