[SDF] (vec3 pos) {
    vec3 p = (fract(pos / cell_radius) - 0.5) * cell_radius;
    return ${SDF}(p);
}

float {
    "Name": "cell_radius",
    "Default": 10.0,
    "Min": 0.001,
    "Max": 30.0
}