[SDF](vec3 p) {
    float c = cos(bending*p.x);
    float s = sin(bending*p.x);
    mat2 m = mat2(c,-s,s,c);
    vec3 q = vec3(m*p.xy, p.z);
    return ${SDF}(q);
}

float {
    "Name": "bending",
    "Default": 1.0,
    "Min": 0.001,
    "Max": 2.0
}