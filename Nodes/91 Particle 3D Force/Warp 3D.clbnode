/* -----------------------------------------------------------------------------
 * This file was automatically generated by a Python script.
 * PLEASE DON'T EDIT IT DIRECTLY, your changes would be overwritten the next time the script is run.
 * Instead, go to "input/Warp.clbtemp" and see https://coollab-art.com/Tutorials/Writing%20Nodes/Particle%20nodes.
 * -----------------------------------------------------------------------------
 */

// To learn how to write nodes, see https://coollab-art.com/Tutorials/Writing%20Nodes/Intro

Particle3D main(Particle3D particle)
{
    if (particle.position.x > 1.)
    {
        particle.position.x = -1.;
    }
    else if (particle.position.x < -1.)
    {
        particle.position.x = 1.;
    }
    if (particle.position.y > 1.)
    {
        particle.position.y = 1.;
    }
    else if (particle.position.y < -1.)
    {
        particle.position.y = -1.;
    }
    ;
    if (particle.position.z > 1.)
    {
        particle.position.z = -1.;
    }
    else if (particle.position.z < -1.)
    {
        particle.position.z = 1.;
    }
    ;
    return particle;
}
