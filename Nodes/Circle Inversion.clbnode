// To learn how to write nodes, see https://coollibs.github.io/lab/Writing%20Nodes/Intro

INPUT float `Raduis`;
INPUT Point2D `Center`;

UV main(UV uv)
{
    float d = distance(`Center`,uv);

    vec2 v = normalize(uv-`Center`);

    return `Center`+v*`Raduis`*`Raduis`/d;
}
