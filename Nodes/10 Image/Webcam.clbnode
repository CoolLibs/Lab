
sRGB main(UV uv) // TODO(TD) Check with color space OpenCV uses
{
    uv /= 2.;
    uv.x /= _webcam_aspect_ratio;
    uv.y *= -1.f;
    uv += 0.5;
    return texture(_webcam, uv).bgr; // conversion BGR -> RGB because OpenCV gets Webcam texture in BRG
}