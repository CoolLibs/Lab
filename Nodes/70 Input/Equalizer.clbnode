// To learn how to write nodes, see https://coollab-art.com/Tutorials/Writing%20Nodes/Intro

INPUT float->float 'Spectrum';

float main(UV uv)
{
    uv = uv * 0.5 + 0.5;
    if (uv.y < 0.)
        return 0.f;
    return 'Spectrum'(uv.x) < uv.y ? 0. : 1.;
}