
// To learn how to write nodes, see https://coollab-art.com/Tutorials/Writing%20Nodes/Intro

INPUT vec3->SignedDistance 'Shape';

bool main(Particle3D particle)
{
    float distance = 'Shape'(particle.position);
    if (distance < 0)
    {
        return false;
    }
    return true;
}
